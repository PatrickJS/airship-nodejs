{
  "name": "airship-nodejs",
  "version": "1.1.0",
  "description": "SDK for Airship",
  "main": "main.js",
  "scripts": {
    "build": "babel index.js > dist.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon --watch index.js --exec babel index.js --out-file dist.js"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0-beta.39",
    "@babel/core": "^7.0.0-beta.39",
    "@babel/preset-env": "^7.0.0-beta.39",
    "nodemon": "^1.14.12"
  },
  "dependencies": {
    "superagent": "^3.8.2"
  }
}
